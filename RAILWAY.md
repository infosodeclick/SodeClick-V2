# Railway reconnect notes

- Root Directory: (empty)
- Build Command: `npm install`
- Start Command: `npm start`
- Health check path: `/health`

This starter keeps compatibility for both:
- `server.js` as runtime main
- `src/server.js` shim for older mappings
